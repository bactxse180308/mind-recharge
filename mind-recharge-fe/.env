VITE_API_BASE_URL=http://localhost:8080
# Native Android build for a physical device connected by USB.
# Use adb reverse so the phone can reach the backend through 127.0.0.1:8080.
VITE_NATIVE_API_BASE_URL=http://127.0.0.1:8080
